import mysql.connector

# Connect to the db and return the connection
DB = mysql.connector.connect(
  host="demo.ui-network.com",
  user="jega",
  password="Ui-Network24*",
  database="ui_network",
)

BV_VALUE = 4000